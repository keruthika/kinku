#include <stdio.h>
int main()
{
    int i,n,count=0;
    printf("enter a number");
    scanf("%d",&n);
    if(n%i==0)
    {
        count++;
            }
            else if(count==1)
            {
                printf("not a composite number");
            }
        else
    {
        printf("composite number");
    }
    return 0;
}
